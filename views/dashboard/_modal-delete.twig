<!--Modal-->
<div class="popup-modal shadow" data-popup-modal="delete-modal-{{ id }}">
    <input type="hidden" name="id" value="{{ id }}">
    <i class="ri-close-line popup-modal__close bg-danger rounded" aria-hidden="true"></i>
    <div class="popup-modal-header">
        <h1 class="font-weight-bold">
            Confirmation de suppression
        </h1>
    </div>
    <div class="popup-modal-body">
        {% if note is not empty %}
            {{ viewAlert('info', note) | raw }}
        {% endif %}
        Etes-vous certain de vouloir supprimer cette entrée de la base de données ?
        <p>
            &gt; <strong>{{ title }}</strong>
        </p>
        Cette action est <strong>irréversible</strong>
    </div>
    <div class="popup-modal-footer">
        <div class="form-group">
            <button type="submit" name="" class="btn btn-danger"><i class="fa fa-check-circle" aria-hidden="true"></i> Oui, je supprime</button>
            <div class="clear"></div>
        </div>
    </div>
</div>
<!--/modal-->