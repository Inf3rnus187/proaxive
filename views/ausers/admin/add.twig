{% extends '_layout/dashboard/layout.twig' %}

{% block title %}Nouvel administrateur{% endblock %}

{% block body %}
    <!-- START .cards -->
    <div class="section--admin">
        <div class="container">
            <div class="admin-content">
                {{ include('_notifications.twig') }}
                <div class="card">
                    <div class="card-header">
                        <h4 class="card-title">Données du compte</h4>
                    </div>
                    <div class="card-body">
                        <div class="admin-form">
                            <form class="form-big" method="post" action="#">
                            {% include 'ausers/admin/_form.twig' with {'inputpassword': 1} %}
                                {{ form.submit('primary', 'Enregistrer') | raw }}
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- END .cards -->
{% endblock %}